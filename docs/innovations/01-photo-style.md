# Innovation 01 — Photo-to-Style (笔迹照相机)

## Goal

Turn one or more phone-camera photographs of a person's handwriting into a
HandWrite prototype pack that downstream generators can consume via
`handwrite.generate(prototype_pack=...)`. The goal is a zero-training,
on-device path from "snap a page of notes" to a personalised glyph library.
